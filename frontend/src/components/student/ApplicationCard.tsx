import React from 'react';
import { Application } from '../../../shared/types';

interface ApplicationCardProps {
  application: Application;
}

export const ApplicationCard: React.FC<ApplicationCardProps> = ({ applica